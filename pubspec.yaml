name: care2wheeler_customer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0 # Updated to resolvable version
  flutter_secure_storage: ^9.2.4 # Updated to resolvable version
  google_maps_flutter: ^2.10.1 # Updated to resolvable version
  geocoding: ^2.1.0
  uuid: ^3.0.7

  cupertino_icons: ^1.0.2
  # qr_code_scanner: ^1.0.1
  mobile_scanner: ^6.0.7

  firebase_core: ^3.12.1
  firebase_messaging: ^15.2.4
  flutter_local_notifications: ^19.0.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Updated to resolvable version


flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
